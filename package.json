{
  "name": "skillsense-ai",
  "version": "1.0.0",
  "description": "Personalized Skill Gap Self-Diagnosis Tool for Students",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && cd ml-service && pip install -r requirements.txt",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:ml\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev:ml": "cd ml-service && python -m uvicorn app.main:app --reload --port 8000",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/username/skillsense-ai"
  },
  "keywords": [
    "skill-assessment",
    "machine-learning",
    "education-technology",
    "hackathon"
  ],
  "author": "SkillSense Team",
  "license": "MIT"
}
